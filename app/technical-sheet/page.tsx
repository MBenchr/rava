import Link from "next/link";
import { getProductCopy, getStartingPrice, productList, siteMeta } from "@/lib/isandre/catalog";
export default function TechnicalSheetPage() { return <main className="page-shell section-space"><Link href="/" className="text-sm underline">Back</Link><p className="eyebrow mt-14">THE FIRST EDITION</p><h1 className="display-title mt-4 text-6xl">Technical sheet</h1><div className="mt-12 grid gap-8 md:grid-cols-3">{productList.map((product) => { const copy = getProductCopy(product.id, "en"); return <article key={product.id} className="border-t border-border pt-5"><h2 className="display-title text-4xl">{copy.name}</h2><p className="mt-2 text-sm text-muted-foreground">{copy.descriptor}</p><dl className="mt-6"><div className="buy-row"><dt>Dimensions</dt><dd>{product.dimensionsLabel ?? "Pending final drawing"}</dd></div><div className="buy-row"><dt>Price</dt><dd>{getStartingPrice(product.id, "en")}</dd></div><div className="buy-row"><dt>Production</dt><dd>{siteMeta.fabricationDelay}</dd></div></dl></article>; })}</div></main>; }
