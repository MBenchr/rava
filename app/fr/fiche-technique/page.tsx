import Link from "next/link";
import { getProductCopy, getStartingPrice, productList } from "@/lib/isandre/catalog";
export default function TechnicalSheetPage() { return <main className="page-shell section-space"><Link href="/fr" className="text-sm underline">Retour</Link><p className="eyebrow mt-14">LA PREMIÈRE ÉDITION</p><h1 className="display-title mt-4 text-6xl">Fiche technique</h1><div className="mt-12 grid gap-8 md:grid-cols-3">{productList.map((product) => { const copy = getProductCopy(product.id, "fr"); return <article key={product.id} className="border-t border-border pt-5"><h2 className="display-title text-4xl">{copy.name}</h2><p className="mt-2 text-sm text-muted-foreground">{copy.descriptor}</p><dl className="mt-6"><div className="buy-row"><dt>Dimensions</dt><dd>{product.dimensionsLabel ?? "Plan final en validation"}</dd></div><div className="buy-row"><dt>Prix</dt><dd>{getStartingPrice(product.id, "fr")}</dd></div><div className="buy-row"><dt>Fabrication</dt><dd>Sur commande · 20 jours ouvrés</dd></div></dl></article>; })}</div></main>; }
